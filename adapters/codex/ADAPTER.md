# Codex Adapter

The installer copies the portable Agent Skills into `$CODEX_HOME/skills` when `CODEX_HOME` is set, otherwise `~/.codex/skills`.

Restart or reload Codex so the skill metadata is discovered.