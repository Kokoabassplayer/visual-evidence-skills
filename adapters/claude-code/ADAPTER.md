# Claude Code Adapter

The installer materializes this as a Claude Code plugin at `~/.claude/skills/visual-evidence` with skills under `skills/` and `.claude-plugin/plugin.json` at the plugin root.

After installation, reload Claude Code plugins if the session is already running.