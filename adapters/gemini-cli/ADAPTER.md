# Gemini CLI Adapter

Gemini CLI does not rely on the same native plugin layout, so the installer creates `~/.gemini/visual-evidence` with `skills/` and a `GEMINI.md` context file explaining how to use the skills and bundled helper.