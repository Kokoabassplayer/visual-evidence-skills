# Generic Agent Skills Adapter

The installer copies plain Agent Skills into `~/.agents/skills`. Any harness that supports the Agent Skills standard can load the folders from there or from an explicit destination passed with `--dest`.