# OpenClaw Adapter

The installer copies the portable Agent Skills into `~/.openclaw/skills` for shared managed skills. Workspace installs can use `--dest <workspace>` and then move or copy the generated `skills/` folder into the active workspace.